/**
 * Metadata helpers — REMOVED.
 *
 * All decorator metadata is now extracted at compile time by the transformer
 * via AST inspection. Runtime Symbol.metadata writes are no longer needed.
 *
 * This file is intentionally empty. The exports (META, setMeta, pushMeta,
 * getClassMetadata) have been removed.
 */
