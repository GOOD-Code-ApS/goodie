---
"@goodie-ts/core": patch
"@goodie-ts/transformer": patch
"@goodie-ts/health": patch
"@goodie-ts/testing": patch
---

Add library bean discovery via `beans.json` manifests and `transformLibrary()` pipeline. Support abstract class tokens in DI container. Replace `workspace:*` with `workspace:^` for proper semver ranges on publish.
